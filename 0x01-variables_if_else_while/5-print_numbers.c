#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
 * main - Entry point
 * This program prints single digit numbers
 * Return: Always 0(Success)
 */
int main(void)
{
	int i;

	for (i = i ; i < 10 ; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
