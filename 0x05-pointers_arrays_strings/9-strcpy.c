#include "main.h"
/**
 * _strcpy - copies a string
 * @dest: to be copied to
 * @src: to be copied
 *
 * Return: dust
 */
char *_strcpy(char *dest, char *src)
{
	int i;

	for (i = 0 ; scr[i] != '\0' ; i++)
	{
		dest[i] = src[i];
	}
	dest[i] = '\0';
	return (dust);
}
