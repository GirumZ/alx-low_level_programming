#include "main.h"
/**
 * mul - multiplies two numbers
 * @a: the first number
 * @b: the second number
 *
 * Return: the result mult
 */
int mul(int a, int b)
{
	int mult;

	mult = a * b;
	return (mult);
}
